on: [push, pull_request, workflow_dispatch]

env:
  # Override problematic WEST_BOARDS_FORMAT from upstream so west boards doesn't crash on {qualifiers}
  WEST_BOARDS_FORMAT: "{name:32s} {arch:10s} {type:10s} {dir}"

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v0.3
